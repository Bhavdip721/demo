{
  "name": "query-desk-workspace",
  "version": "1.0.0",
  "description": "query desk project monorepo",
  "type": "module",
  "scripts": {
    "website": "pnpm --filter website run dev",
    "admin": "pnpm --filter admin run dev",
    "client-admin": "pnpm --filter client-admin run dev"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.2.2",
    "tailwindcss": "^4"
  }
}
