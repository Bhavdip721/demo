# @query-desk/ui

Shared shadecn/ui component library for Query Desk monorepo.

## Installation

Components are automatically available to all apps in the workspace through pnpm.

## Usage

```tsx
import { Button } from "@query-desk/ui/components/Button";
```

## Adding Components

Run shadecn-ui CLI in this directory to add new components.
